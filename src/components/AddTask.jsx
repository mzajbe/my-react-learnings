/* eslint-disable no-unused-vars */
import React from 'react';

const AddTask = () => {
    return (
        <>
            <input type="text" />
            <button>Add</button>
        </>
    );
};

export default AddTask;